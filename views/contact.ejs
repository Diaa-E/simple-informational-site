<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple Informational Site - Contact Us</title>
    <link rel="stylesheet" href="/css/common.css">
    <link rel="stylesheet" href="/css/newForm.css">
    <link rel="stylesheet" href="/css/contact.css">
    <link rel="shortcut icon" href="/images/favicon.svg" type="svg/xml">
    <script type="module" src="/js/contact.js" defer></script>
</head>
<body>
    <%- include("components/navbar", {activeTab: "Contact Us"}) %>
    <div class="content background">
        <h1 class="page-title">
            <span class="hidden-text">Contact Us</span>
            <span class="visible-text"></span>
        </h1>
        <form class="new-form" action="/contact" method="POST">
            <% if (error) {%>
                <p class="form-error"><%= error.msg %></p>
            <% } %>
            <p class="note">Required fields are marked with a (*)</p>

            <div class="form-group">
                <label class="field-label" for="name">Name</label>
                <input value="<%= data.name %>" class="textbox" type="text" name="name" id="name">
            </div>

            <div class="form-group">
                <label class="field-label" for="email">Email Address</label>
                <input value="<%= data.email %>" required class="textbox" type="email" name="email" id="email">
            </div>

            <div class="form-group">
                <label class="field-label" for="message">Message</label>
                <textarea required class="textbox" rows="6" name="message" id="message"><%= data.message %></textarea>
            </div>

            <button class="button button-primary button-submit" type="submit">Send</button>
            <% if (success) { %>
                <p class="success-message">Message Sent.</p>
            <% } %>
        </form>
    </div>
    <%- include("components/footer", {}) %>
</body>
</html>