<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple Informational Site - Authors</title>
    <link rel="stylesheet" href="/css/common.css">
    <link rel="stylesheet" href="/css/books.css">
    <link rel="shortcut icon" href="/images/favicon.svg" type="svg/xml">
</head>
<body>
    <%- include("components/navbar", { activeTab: "Books" }) %>
    <div class="content background">
        <h1 class="page-title">Books</h1>
        <a class="button-link" href="/books/new">Add a New Author</a>
        <form class="search-form" action="/books/search" method="GET">
            <input class="textbox" name="title" id="title" type="search">
            <button class="button button-primary" type="submit">Search</button>
        </form>
        <% if (allBooks !== null) {%>
            <p class="results-title">Found <%= allBooks.length %> results for "<%= keyword %>"</p>
            <ul class="entry-list">
                <% allBooks.forEach(book => { %>
                    
                    <li>
                        <a class="entry-link" href="/books/<%=book.id%>"><%= book.name %></a>
                    </li>
                <% }) %>
            </ul>
        <% } %>
    </div>
    <%- include("components/footer", {}) %>
</body>
</html>