<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add a New Author</title>
    <link rel="stylesheet" href="/css/common.css">
    <link rel="stylesheet" href="/css/newForm.css">
    <link rel="stylesheet" href="/css/authors.css">
    <link rel="shortcut icon" href="/images/favicon.svg" type="svg/xml">
    <script type="module" src="/js/addAuthor.js" defer></script>
</head>
<body>
    <%- include("components/navbar", { activeTab: "Authors" }) %>
    <div class="content background">
        <h1 class="page-title">
            <span class="hidden-text">Add a New Author</span>
            <span class="visible-text"></span>
        </h1>
        <form class="new-form" action="/authors/new" method="POST">
            <% if (error) {%>
                <p class="form-error"><%= error.msg %></p>
            <% } %>
            <p class="note">Required fields are marked with a (*)</p>

            <div class="form-group">
                <label class="field-label" for="name">Name</label>
                <input value="<%= data.name %>" required class="textbox" type="text" name="name" id="name">
            </div>

            <div class="form-group">
                <label class="field-label" for="bio">Bio</label>
                <textarea required class="textbox" rows="6" name="bio" id="bio"><%= data.bio %></textarea>
            </div>

            <button class="button button-primary button-submit" type="submit">Add</button>
        </form>
    </div>
    <%- include("components/footer", {}) %>
</body>
</html>